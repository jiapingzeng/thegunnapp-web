doctype html
html
  head
    meta(charset='utf-8')
    meta(name='viewport', content='width=device-width, initial-scale=1')
    title=title
    link(rel='stylesheet', href='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css')
  body
    div.container
      h2
        a(href='/') TheGunnApp
      h4 (Only shows alternate schedules. For regular schedule, see 
        a(href='http://gunn.pausd.org/bell-schedule') here
        | )
      h5 This project was last updated on 9/23/2017 and might not continue to receive regular updates. If you find it useful, please 
        a(href='mailto://jiaping@gisar.me') contact me
        | .
      h5  Meanwhile, feel free to check out 
        a(href='https://facebook.com/thegunnapp') TheGunnApp bot for Facebook Messenger
        | .
      hr
      if data
        if data.items[0]
          for event in data.items
            .panel.panel-default
              .panel-heading
                h4=moment(event.start.date).format('ddd, MMM Do, YYYY')
              .panel-body
                b=event.summary
              table.table
                if format(event.description)
                  for period in format(event.description)
                    tr
                      td=period.substring(0, period.indexOf('|'))
                      td=period.substring(period.indexOf('|') + 1)
                else
                  tr
                    td Error loading schedule
        else
          h4 Might be a regular schedule day. Would you like to 
            a(href='/list') view all days
            | ?
      else
        h4 Error retriving data
      hr
      a(href='/today') view today
      |  | 
      a(href='/list') view all
      footer
        small Only displays special schedule. Use at your own risk.
        br
        small &copy; 2017 Jiaping Zeng